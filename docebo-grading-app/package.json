{
  "name": "docebo-grading-app",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Docebo assignment grading UI (server + client)",
  "scripts": {
    "dev": "concurrently \"npm run --prefix server start\" \"npm start --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}